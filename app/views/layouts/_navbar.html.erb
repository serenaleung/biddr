<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Biddr</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">

        <li><span class="sr-only">(current)</span></a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><form class="navbar-form">
          <div>
            <button type="button" class="btn btn-default"><%= link_to('New Idea', new_auction_path) %></button>
          </div>
        </form></li>
        <% if user_signed_in? %>
          <ul class="nav navbar-nav navbar-right">
            <li><%#= link_to "#{current_user.name}, Logout", sessions_path, method: :delete, data: {confirm: 'Logout?'} %></li>
          </ul>
        <% else %>
          <li><%= link_to('Sign Up', new_user_path) %></li>
          <li><%= link_to('Login', new_session_path) %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>
